/*
new goal
1: fix header to make it stretch from left to right screen, there should be no padding or margin at the very top
2: fix footer to divide detail and shortcut ratio to 60:40, the shortcut should have text hyperlink clickable but not a button.
3: make a transaction: make a new card appear or hover above cart that asks to proceed to payment button and later
3.1(choice): click proceed payment, negative amount warning, higher amount -> click proceed-> then sukli( order status = processing), cancel button ->= order status = pending
3.2(choice): click cancel ->= order status(pending)
3.3: customer orders with proceed payment button on pending status and cancel button to status cancel, orders processing/completed check receipt button

4: idk if mailer is setup, if setup add receipt downloadable
5: email verify(is it required????) 
6: add hover effect on charts to show which products as well for the chart line


fixes
1: product add category dropdown choices
2: added name and description on item table(check nodetest001.sql)
3: revamp customer ui
4: revamp profile button
*/